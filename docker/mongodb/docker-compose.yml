version: '3.8'

services:
  mongodb:
    build: .
    container_name: mongodb-8.2.4
    ports:
      - "27028:27028"
    volumes:
      - ./data:/data/db
    restart: unless-stopped
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=password
